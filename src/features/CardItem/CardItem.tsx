import Image from 'next/image';
import { FormattedCar } from '@/entities/types/Car';

interface CardItemProps {
    card: FormattedCar;
}

const CardItem = ({card}: CardItemProps) => {
return (
    <div>
        
    </div>
)
}

export default CardItem;